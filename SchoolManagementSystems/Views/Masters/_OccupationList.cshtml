@model Entity.occupationviewmodel
<div id="result" class="table-responsive">
    <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.occupationid)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.occupationnm)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.status)
                </th>
                @*<th>
                        @Html.DisplayNameFor(model => model.academicyear)
                    </th>*@
                <th class="edt">
                    @Html.DisplayName("Action")
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model._occupationlist)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Occupationid)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Occupationnm)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.status)
                    </td>
                    @*<td>
                            @Html.DisplayFor(modelItem => item.academicyear)
                        </td>*@
                    <td>
                        <input type="button" id="@item.Occupationid" class="btn btn-default btn_edit edit" value="Edit" />
                        @*<input type="button" id="@item.Occupationid" class="btn btn-default btn_delete delete" value="Delete" />*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
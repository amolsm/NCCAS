@model Entity.lib_JTypemodule
<div id="result" class="table-responsive">
    <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.jTypeId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.JTypeName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.status)
                </th>
                <th class="edt">
                    @Html.DisplayName("Action")
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model._JTypelist)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.JTypeid)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.JTypeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        <input type="button" id="@item.JTypeid" class="btn btn-default btn_edit edit" value="Edit" />
                        @*<input type="button" id="@item.Countryid" class="btn btn-default btn_delete delete" value="Delete" />*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>